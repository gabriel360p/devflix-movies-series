import styled from "styled-components";

export const Container = styled.div`
  width:100%;
  padding: 10px clamp(.25rem, 2vw, 1.5rem);
`;
export const CarrouselMainTitle = styled.h3`
  padding-left:15px;
  color:White;
  font-size: clamp(1.15rem, 2.5vw, 1.5rem);
  margin-top: 1.5rem;

`
